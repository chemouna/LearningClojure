(ns learning.core)

(defn -main
 []
 (println "Hello...")
)

;(defn isPalindrome? [s]
;  (= s (apply str (reverse s) )))

;(defn -main
;  [& args]
;  (println "Started isPalindrome Calculation...")
;  (print " : ")
;  (println (isPalindrome? 9994))
;  )
